{
  "name": "law-enforcement",
  "version": "1.0.0",
  "private": true,
  "description": "Repo root entry point. Serves the Red Lab Studios site so a host can deploy this branch without needing a root-directory setting.",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node roblox-studio-site/server.js"
  }
}
